# Legacy migrations archive

This directory stores archived SQL and README references for migrations that
were removed from runtime service boot paths.

The service-specific folders are historical inputs for migration planning,
incident response, and data-migration tooling. Runtime services should not load
these migrations directly.
